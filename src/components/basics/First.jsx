import React from 'react'

export default function First() {

    const msg = 'Seja bem vindo(a) ao meu primeiro projeto em React!'

    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{ msg }</p>
        </div>
    )
}

// Componentes Funcionais - Componentes baseados em funções, como acima.
